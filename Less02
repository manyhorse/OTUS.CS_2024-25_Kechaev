;по задумке 0100 (4) должно превращаться в 1011 (11). У меня сохраняется -15... как я понял, это потому что ПЕРЕД моим числом 1011 проставлены нули, которые тоже инвертируются

.ORIG x3000
LD R0, NUM
LOOP
NOT R0, R0
DONE HALT
NUM .FILL x4
.END

Итого
0100 -> 1011 -> 1100
